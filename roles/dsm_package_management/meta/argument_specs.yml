---
argument_specs:
  main:
    short_description: "Install and configure DSM packages"
    options:
      dsm_package_management_host:
        description: Target host address
        type: str
        required: true
      dsm_package_management_username:
        description: Authentication username
        type: str
      dsm_package_management_password:
        description: Authentication password
        type: str
      dsm_package_management_api_key:
        description: API key for authentication
        type: str
      dsm_package_management_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      dsm_package_management_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
